# Paths are resolved relative to this configuration file.
camera_file=../data/camera.txt
imu_file=../data/imu.txt
output_file=../data/result.txt

# Bounded queue and matching parameters.
queue_capacity=3
max_delta_us=500

# Delays simulate a 100 Hz camera and a 400 Hz IMU.
camera_delay_us=10000
imu_delay_us=2500
