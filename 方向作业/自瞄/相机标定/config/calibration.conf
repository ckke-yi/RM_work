# Paths are relative to the directory where the command is launched.
# Run from the project root, or replace them with absolute paths.
input_video=input/WIN_20260904_02_30_18_Pro.mp4
output_dir=output

# The generator is configured as 10 x 7 squares, so OpenCV sees 9 x 6 corners.
board_cols=9
board_rows=6
square_size_mm=16.0

# Use views across the complete 45-second recording.
sample_step_frames=30
max_samples=60
